package com.smartwizard.viewholders

import android.view.View
import com.smartwizard.StatusVO

class StatusViewHolder(var view: View) : BaseViewHolder<StatusVO>(view)
{
    override fun setData(mData: StatusVO)
    {
//        view.gaugeView.pointStartColor = R.color.colorRed
//        view.gaugeView.pointEndColor = R.color.colorRed

//        view.gaugeView.pointSize = 200
    }
}